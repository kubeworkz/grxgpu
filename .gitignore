/build*
/.*
!/.github/
!/.github/**
# Archived/superseded docs — kept on disk, never tracked
docs/archives/

# Source export artifact (never tracked)
grxgpu-src.tar.gz

# Freebuff harness workspace state (never tracked; gitlink here breaks CI submodule checkout)
/.freebuff/
